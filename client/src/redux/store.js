import { configureStore } from '@reduxjs/toolkit';
import reducer from './reducers'
 // this is a starter kit function that does a bunch of boiler plate stuff
export default configureStore({reducer});